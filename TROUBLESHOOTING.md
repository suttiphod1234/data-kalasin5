# 🔧 วิธีแก้ไขปัญหา: ข้อมูลไม่เข้า Google Sheet

## ⚠️ ปัญหา
ข้อมูลจากฟอร์มไม่ถูกบันทึกลง Google Sheet แม้จะกรอกข้อมูลและกดบันทึกแล้ว

## ✅ วิธีแก้ไข (ทำตามลำดับ)

### ขั้นตอนที่ 1: อัปเดตโค้ดใน Google Apps Script

1. **เปิด Google Sheet**
   - ไปที่: https://docs.google.com/spreadsheets/d/1t2rCVoEQatp9FMvjoR0UFUNkWgGP7Y27YTrzHPO8PQ8/edit

2. **เปิด Apps Script Editor**
   - คลิก **ส่วนขยาย** (Extensions) > **Apps Script**

3. **ลบโค้ดเดิมทั้งหมด**
   - เลือกโค้ดทั้งหมดใน Editor (Ctrl+A หรือ Cmd+A)
   - กด Delete

4. **คัดลอกโค้ดใหม่**
   - เปิดไฟล์ `code.gs` ในโปรเจค
   - คัดลอกโค้ดทั้งหมด
   - วางลงใน Apps Script Editor

5. **บันทึก**
   - คลิก **บันทึก** (💾) หรือกด Ctrl+S

---

### ขั้นตอนที่ 2: Deploy เวอร์ชันใหม่

1. **เปิด Manage Deployments**
   - คลิก **Deploy** > **Manage deployments**

2. **แก้ไข Deployment ปัจจุบัน**
   - คลิกไอคอน **✏️ (แก้ไข)** ที่ deployment ที่มีอยู่

3. **สร้าง Version ใหม่**
   - ที่ **Version** เลือก **New version**
   - ใส่ Description: "แก้ไขปัญหาข้อมูลไม่เข้า Sheet"

4. **Deploy**
   - คลิก **Deploy**
   - รอสักครู่
   - คลิก **Done**

5. **คัดลอก URL** (ถ้ามีการเปลี่ยน)
   - ถ้า Web App URL เปลี่ยน ให้คัดลอก URL ใหม่
   - อัปเดตใน `script.js` บรรทัดที่ 2

---

### ขั้นตอนที่ 3: ทดสอบใน Apps Script (Optional แต่แนะนำ)

1. **เลือกฟังก์ชัน testDoPost**
   - ที่ dropdown ด้านบน เลือก `testDoPost`

2. **Run**
   - คลิกปุ่ม **▶️ Run**
   - อนุญาตสิทธิ์ถ้าถาม

3. **ดู Log**
   - คลิก **Execution log** ด้านล่าง
   - ควรเห็น: `{"result":"success",...}`

4. **ตรวจสอบ Google Sheet**
   - กลับไปที่ Google Sheet
   - ควรเห็นข้อมูลทดสอบในแถวที่ 2

---

### ขั้นตอนที่ 4: ทดสอบจากฟอร์มจริง

1. **เปิดฟอร์ม**
   - https://suttiphod1234.github.io/data-kalasin5/

2. **Hard Refresh**
   - กด Cmd+Shift+R (Mac) หรือ Ctrl+Shift+R (Windows)
   - หรือเปิดใน Incognito/Private Mode

3. **กรอกข้อมูลทดสอบ**
   - รหัสแกนนำ: TEST002
   - เลขบัตรประชาชน: 1234567890123
   - อำเภอ: อำเภอห้วยผึ้ง
   - ตำบล: ตำบลหนองอีบุตร
   - หมู่ที่: 1
   - บ้านเลขที่: 32
   - ชื่อ-สกุล: ทดสอบ ระบบ
   - จำนวนผู้มาใช้สิทธิ์: 5

4. **กดบันทึก**
   - กดปุ่ม "บันทึกข้อมูล"
   - รอสักครู่

5. **ตรวจสอบผล**
   - ถ้าสำเร็จ: จะขึ้น Modal "บันทึกข้อมูลสำเร็จ!"
   - ถ้าผิดพลาด: จะแสดง error message

6. **ตรวจสอบ Google Sheet**
   - กลับไปที่ Google Sheet
   - Refresh หน้า
   - ควรเห็นข้อมูลใหม่

---

## 🔍 การ Debug

### ถ้ายังไม่ทำงาน ให้ตรวจสอบ:

#### 1. **ตรวจสอบ Console ในเบราว์เซอร์**
   - กด F12 เพื่อเปิด Developer Tools
   - ไปที่แท็บ **Console**
   - กดบันทึกข้อมูล
   - ดู error message

#### 2. **ตรวจสอบ Network**
   - ในแท็บ **Network**
   - กดบันทึกข้อมูล
   - หา request ไปที่ `script.google.com`
   - คลิกดู Response
   - ควรเห็น `{"result":"success",...}`

#### 3. **ตรวจสอบ Apps Script Logs**
   - ใน Apps Script Editor
   - คลิก **Executions** (ด้านซ้าย)
   - ดู execution log ล่าสุด
   - ถ้ามี error จะแสดงที่นี่

#### 4. **ตรวจสอบ URL**
   - ใน `script.js` บรรทัดที่ 2
   - ตรวจสอบว่า `scriptURL` ถูกต้อง
   - ต้องลงท้ายด้วย `/exec`

#### 5. **ตรวจสอบ Sheet ID**
   - ใน `code.gs` บรรทัดที่ 10
   - ตรวจสอบว่า `sheetId` ถูกต้อง
   - ต้องตรงกับ URL ของ Google Sheet

---

## 📋 Checklist

- [ ] อัปเดตโค้ดใน Apps Script แล้ว
- [ ] Deploy เวอร์ชันใหม่แล้ว
- [ ] ทดสอบ testDoPost() สำเร็จ
- [ ] Google Sheet มีหัวคอลัมน์ครบ 10 คอลัมน์
- [ ] scriptURL ใน script.js ถูกต้อง
- [ ] sheetId ใน code.gs ถูกต้อง
- [ ] folderId ใน code.gs ถูกต้อง
- [ ] ทดสอบส่งข้อมูลจากฟอร์มสำเร็จ

---

## 🆘 ยังแก้ไม่ได้?

ถ้าทำทุกขั้นตอนแล้วยังไม่ได้ ให้:

1. **ส่ง Screenshot ของ:**
   - Console error (F12 > Console)
   - Network response (F12 > Network)
   - Apps Script execution log

2. **ส่งข้อมูล:**
   - scriptURL ที่ใช้
   - sheetId ที่ใช้
   - Error message ที่เห็น

3. **ลอง Deploy ใหม่ทั้งหมด:**
   - Delete deployment เดิม
   - สร้าง deployment ใหม่
   - อัปเดต URL ใน script.js
   - Push ขึ้น GitHub

---

## ✅ เมื่อแก้ไขสำเร็จ

คุณจะเห็น:
- ✅ ข้อมูลถูกบันทึกใน Google Sheet
- ✅ รูปภาพถูกอัปโหลดไปยัง Google Drive
- ✅ ลิงก์รูปภาพปรากฏในคอลัมน์ image_url
- ✅ Modal "บันทึกข้อมูลสำเร็จ!" แสดงขึ้น
- ✅ ฟอร์มถูก reset พร้อมกรอกข้อมูลใหม่
