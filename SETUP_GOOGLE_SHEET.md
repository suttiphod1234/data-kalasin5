# 📊 คู่มือการตั้งค่า Google Sheet สำหรับฟอร์มเก็บข้อมูล

## ขั้นตอนที่ 1: สร้าง Google Sheet

1. เปิด [Google Sheets](https://sheets.google.com)
2. คลิก **+ สร้าง** เพื่อสร้าง Spreadsheet ใหม่
3. ตั้งชื่อ Sheet เช่น **"ข้อมูลแกนนำหมู่บ้าน"**

## ขั้นตอนที่ 2: สร้างหัวตาราง (Headers)

ในแถวแรก (Row 1) ให้ใส่หัวคอลัมน์ดังนี้:

| A | B | C | D | E | F | G | H | I | J |
|---|---|---|---|---|---|---|---|---|---|
| timestamp | leader_id | national_id | district | sub_district | village_moo | house_number | fullname | voters_count | image_url |

### คำอธิบายแต่ละคอลัมน์:
- **timestamp**: วันเวลาที่บันทึกข้อมูล (สร้างอัตโนมัติ)
- **leader_id**: รหัสแกนนำ
- **national_id**: เลขบัตรประจำตัวประชาชน 13 หลัก
- **district**: อำเภอ
- **sub_district**: ตำบล
- **village_moo**: หมู่บ้าน (หมู่ที่)
- **house_number**: บ้านเลขที่
- **fullname**: ชื่อ-สกุล
- **voters_count**: จำนวนผู้มาใช้สิทธิ์
- **image_url**: ลิงก์รูปภาพบัตรประชาชน (สร้างอัตโนมัติ)

## ขั้นตอนที่ 3: คัดลอก Sheet ID

1. ดูที่ URL ของ Google Sheet
2. URL จะมีรูปแบบ: `https://docs.google.com/spreadsheets/d/[SHEET_ID]/edit`
3. คัดลอก **SHEET_ID** (ส่วนที่อยู่ระหว่าง `/d/` และ `/edit`)

**ตัวอย่าง:**
```
URL: https://docs.google.com/spreadsheets/d/1t2rCVoEQatp9FMvjoR0UFUNkWgGP7Y27YTrzHPO8PQ/edit
SHEET_ID: 1t2rCVoEQatp9FMvjoR0UFUNkWgGP7Y27YTrzHPO8PQ
```

## ขั้นตอนที่ 4: สร้าง Google Drive Folder สำหรับเก็บรูปภาพ

1. เปิด [Google Drive](https://drive.google.com)
2. คลิก **+ ใหม่** > **โฟลเดอร์**
3. ตั้งชื่อโฟลเดอร์ เช่น **"รูปบัตรประชาชนแกนนำ"**
4. คลิกขวาที่โฟลเดอร์ > **แชร์**
5. เปลี่ยนเป็น **"ทุกคนที่มีลิงก์สามารถดูได้"**
6. ดูที่ URL ของโฟลเดอร์
7. คัดลอก **FOLDER_ID** (ส่วนที่อยู่หลัง `/folders/`)

**ตัวอย่าง:**
```
URL: https://drive.google.com/drive/folders/1ISsWh0UTfH_F2NrhTIefQzwK7Qe_up0X
FOLDER_ID: 1ISsWh0UTfH_F2NrhTIefQzwK7Qe_up0X
```

## ขั้นตอนที่ 5: เปิด Apps Script Editor

1. ใน Google Sheet ที่สร้างไว้
2. คลิก **ส่วนขยาย** (Extensions) > **Apps Script**
3. จะเปิดหน้าต่างใหม่ขึ้นมา

## ขั้นตอนที่ 6: วาง Code ลงใน Apps Script

1. ลบโค้ดเดิมทั้งหมดออก
2. คัดลอกโค้ดจากไฟล์ `code.gs` ในโปรเจค
3. วางลงใน Apps Script Editor
4. **แก้ไข** `sheetId` และ `folderId` ให้ตรงกับของคุณ:

```javascript
var sheetName = 'Sheet1'
var scriptProp = PropertiesService.getScriptProperties()
// UPDATED: ใส่ Folder ID และ Sheet ID ของคุณ
var folderId = 'ใส่ FOLDER_ID ของคุณที่นี่' 
var sheetId = 'ใส่ SHEET_ID ของคุณที่นี่'
```

5. คลิก **บันทึก** (💾)

## ขั้นตอนที่ 7: Deploy Web App

1. คลิก **Deploy** > **New deployment**
2. คลิกไอคอน **⚙️ (เฟือง)** ข้างๆ "Select type"
3. เลือก **Web app**
4. ตั้งค่าดังนี้:
   - **Description**: "ฟอร์มเก็บข้อมูลแกนนำ v1"
   - **Execute as**: **Me** (อีเมลของคุณ)
   - **Who has access**: **Anyone** (ทุกคน)
5. คลิก **Deploy**
6. คลิก **Authorize access** (อนุญาตการเข้าถึง)
7. เลือกบัญชี Google ของคุณ
8. คลิก **Advanced** > **Go to [ชื่อโปรเจค] (unsafe)**
9. คลิก **Allow** (อนุญาต)

## ขั้นตอนที่ 8: คัดลอก Web App URL

1. หลังจาก Deploy สำเร็จ จะได้ **Web app URL**
2. คัดลอก URL ทั้งหมด (จะยาวมาก)

**ตัวอย่าง:**
```
https://script.google.com/macros/s/AKfycbwqbfw62KXxbTKmn9HD8fjUV6_BqpjcEtt65U8usFLNY6q37dzn8GN0AcemZrb2Gp3s0Q/exec
```

## ขั้นตอนที่ 9: อัปเดต URL ในไฟล์ script.js

1. เปิดไฟล์ `script.js` ในโปรเจค
2. หาบรรทัดที่ 2:
```javascript
const scriptURL = 'https://script.google.com/macros/s/AKfycbwqbfw62KXxbTKmn9HD8fjUV6_BqpjcEtt65U8usFLNY6q37dzn8GN0AcemZrb2Gp3s0Q/exec';
```
3. **แทนที่** URL เดิมด้วย URL ที่คุณคัดลอกมาจากขั้นตอนที่ 8
4. **บันทึกไฟล์**

## ขั้นตอนที่ 10: Push ขึ้น GitHub

```bash
git add script.js
git commit -m "อัปเดต Google Apps Script URL"
git push origin gh-pages
```

## ✅ เสร็จสิ้น!

ตอนนี้ฟอร์มของคุณพร้อมใช้งานแล้ว! เมื่อมีคนกรอกข้อมูล:
- ข้อมูลจะถูกบันทึกลง Google Sheet
- รูปภาพจะถูกอัปโหลดไปยัง Google Drive
- ลิงก์รูปภาพจะถูกบันทึกใน column `image_url`

## 🔧 การทดสอบ

1. เปิดฟอร์มที่ https://suttiphod1234.github.io/data-kalasin5/
2. กรอกข้อมูลทดสอบ
3. ตรวจสอบว่าข้อมูลปรากฏใน Google Sheet
4. ตรวจสอบว่ารูปภาพถูกอัปโหลดไปยัง Google Drive

## ⚠️ หมายเหตุสำคัญ

- **อย่าแชร์ Web App URL** กับคนอื่น (ใช้สำหรับฟอร์มเท่านั้น)
- ถ้าต้องการแก้ไข `code.gs` ให้ Deploy ใหม่และอัปเดต URL
- ตรวจสอบว่า Google Sheet และ Drive Folder มีสิทธิ์การเข้าถึงที่ถูกต้อง

## 🆘 แก้ปัญหา

### ข้อมูลไม่เข้า Sheet
- ตรวจสอบ `sheetId` ว่าถูกต้อง
- ตรวจสอบว่าหัวคอลัมน์ตรงกับที่กำหนด
- เปิด Apps Script > **Executions** เพื่อดู error log

### รูปภาพไม่อัปโหลด
- ตรวจสอบ `folderId` ว่าถูกต้อง
- ตรวจสอบว่าโฟลเดอร์มีสิทธิ์ "Anyone with link can view"

### Error: "Access denied"
- ตรวจสอบว่า Deploy เป็น "Anyone" ได้
- ลอง Authorize access ใหม่อีกครั้ง
